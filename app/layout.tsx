import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Thuba Tlala - Fresh from Farm to Table",
  description:
    "Community-driven grocery platform connecting you with local farmers and butchers through our innovative Stockvel model. Fresh, affordable, and ethical food sourcing.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
